[pytest]
testpaths = tests/unit
filterwarnings =
    # Third-party noise from the docker image (boto3/botocore vendored requests
    # use invalid escape sequences in docstrings). Not our code.
    ignore::DeprecationWarning:botocore.*
    ignore::DeprecationWarning:boto3.*
    ignore::DeprecationWarning:requests.*

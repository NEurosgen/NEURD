# Runtime dependencies for the slim single-soma segmentation pipeline.
# Verified env: Python 3.12.13, numpy 2.4.6, open3d 0.19+, meshparty 2.x.
# Comments and blank lines are stripped by setup.py's get_install_requires().
#
# NOT LISTED HERE, must be installed first with --no-deps:
#     pip install --no-deps mesh_processing_tools==1.0.4
# Its metadata pins open3d==0.11.2, which conflicts with the modern open3d this
# pipeline needs. scripts/install_local.sh does this for you.

# --- core ---
numpy>=2,<3
scipy
pandas>=2
networkx>=3
matplotlib>=3.7
h5py
tqdm
scikit-learn>=1.3

# --- mesh stack ---
trimesh>=4
meshparty>=2.0
# open3d: in-process decimation + Poisson, the shipped-by-default path (neurd/_mesh_ops.py).
open3d>=0.19
# pykdtree: submesh_ops.original_faces, matching tu's KDTree behaviour byte-for-byte.
pykdtree>=1.4

# --- NEURD-ecosystem author packages ---
datasci-stdlib-tools>=1.0.1
machine-learning-tools>=1.0.0
graph-nx-tools>=1.0.0
neuron_morphology_tools>=1.0.1
code_structure_tools>=1.0.2

# Local install pinning for Python 3.12 (Phase 3).
# Use together with scripts/install_local.sh — that script installs
# mesh_processing_tools with --no-deps first to bypass its `open3d==0.11.2` pin.
#
# Verified env: Python 3.12.13, numpy 2.4.6, open3d 0.19+, meshparty 2.x.

# --- core pipeline ---
numpy>=2,<3
scipy
pandas>=2
networkx>=3
matplotlib>=3.7
h5py
tqdm
scikit-learn>=1.3

# --- mesh stack ---
trimesh>=4
meshparty>=2.0
open3d>=0.19
pykdtree>=1.4
pymeshfix>=0.16

# --- author packages (NEURD ecosystem) ---
datasci-stdlib-tools>=1.0.1
machine-learning-tools>=1.0.0
graph-nx-tools>=1.0.0
neuron_morphology_tools>=1.0.1
code_structure_tools>=1.0.2

# --- dev / test ---
pytest
pytest-mock
